"""
napari_evi_reader
=================
A minimal napari plugin that teaches napari how to open `.evi` files —
a custom volumetric image format backed by NumPy's binary array format.

This package is discovered by napari via the `npe2` manifest system.
See ``napari.yaml`` for the contribution declarations.
"""
